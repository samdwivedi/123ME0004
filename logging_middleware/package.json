{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware for notification system - sends structured logs to evaluation service",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "format": "prettier --write src/"
  },
  "keywords": ["logging", "middleware", "notifications"],
  "license": "MIT",
  "dependencies": {
    "axios": "^1.7.2"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.12",
    "eslint": "^9.3.0",
    "prettier": "^3.2.5"
  }
}
